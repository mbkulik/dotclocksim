
int dotRad = 25;

void one() {
  ellipse(30,30,dotRad, dotRad);
}

void two() {
  ellipse(0,30, dotRad, dotRad);
  ellipse(60, 30, dotRad, dotRad);
}

void three() {
  ellipse(0,30, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
}

void four() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
}

void five() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
}

void six() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
}

void seven() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
}

void eight() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(0,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
}

void nine() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(0,30, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
}

void ten() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(0,30, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
  ellipse(90,30, dotRad, dotRad);
}

void eleven() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(0,30, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
  ellipse(90,0, dotRad, dotRad);
  ellipse(90,60, dotRad, dotRad);
}

void twelve() {
  ellipse(0,0, dotRad, dotRad);
  ellipse(30,0, dotRad, dotRad);
  ellipse(60,0, dotRad, dotRad);
  ellipse(0,60, dotRad, dotRad);
  ellipse(30,60, dotRad, dotRad);
  ellipse(60,60, dotRad, dotRad);
  ellipse(0,30, dotRad, dotRad);
  ellipse(30,30, dotRad, dotRad);
  ellipse(60,30, dotRad, dotRad);
  ellipse(90,0, dotRad, dotRad);
  ellipse(90,30, dotRad, dotRad);
  ellipse(90,60, dotRad, dotRad);
}